name: Auto Update App Rates (Senti & BuildTally)

on:
  schedule:
    - cron: '0 6 * * *'   # 06:00 UTC = 09:00 EAT, daily
  workflow_dispatch: {}    # lets you trigger it manually from the Actions tab

permissions:
  contents: write

concurrency:
  group: rates-sync
  cancel-in-progress: false

jobs:
  update-rates:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install requests beautifulsoup4 pdfplumber

      - name: Run Senti rate sync
        run: python senti/update_rates.py

      - name: Run BuildTally rate sync & validation
        run: python buildtally/update_rates.py --bump-auto

      - name: Commit and push if changed
        run: |
          git config user.name "Tech2U Rate Bot"
          git config user.email "bot@tech2u.com"
          git add senti/rates.json buildtally/rates.json
          if git diff --cached --quiet; then
            echo "No rate changes today."
          else
            git commit -m "auto: sync Senti & BuildTally rates"
            git push
          fi
